# When running pytest you can use the flags -v to verbose,
# -x to stop in the first failure,
# -l to show local variables.

pytest code/test_1_hypothesis.py -vlx

pytest code/test_2_hypothesis.py -vlx

pytest code/test_3_contracts.py -vlx

pytest code/test_4_contracts.py -vlx
